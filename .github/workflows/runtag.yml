name: TestRelease
on:
  release:
    types: 
      - published
  push:
    tags:
      - v1.**

jobs:
  testing:
    runs-on: ubuntu-latest
    steps: 
      - name: print-github-context
        run: echo '${{ toJSON(github) }}'
        shell: bash
