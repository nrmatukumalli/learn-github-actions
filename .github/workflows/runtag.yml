name: TestRelease
on:
  release:
    types:
      - published
  push:
    tags:
      - '**'
jobs:
  testing:
    runs-on: ubuntu-latest
    steps: 
      - name: print-github-context
        run: echo '${{ toJSON(github) }}'
        shell: bash
